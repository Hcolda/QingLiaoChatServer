#include "socket.h"

namespace qls
{
    // Socket::Socket(asio::ip::tcp::socket&& s, asio::ssl::context& ctx):
    //     stream(std::move(s), ctx) {}

    // Socket::Socket(Socket&& s) noexcept:
    //     stream(std::move(s)) {}
}
